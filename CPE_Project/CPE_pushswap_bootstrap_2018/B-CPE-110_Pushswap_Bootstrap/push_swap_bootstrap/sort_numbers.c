/*
** EPITECH PROJECT, 2018
** sort_numbers
** File description:
** function that sort_numbers
*/

#include "pimp_my_algo.h"
#include "struct.h"

void push()
{
    node_t *head = NULL;
    head = malloc(sizeof(node_t));

    if (head = NULL) {
        return (1);
    }
    head->value = 1;
    head->next = malloc(sizeof(node_t));
    head->next->value = 2;
    head->next->value = NULL;
}